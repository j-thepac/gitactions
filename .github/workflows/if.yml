name: ifcondition
on: [workflow_dispatch]
jobs:
  myjobname:
    runs-on: ubuntu-latest
    if: github.event_name== 'workflow_dispatch'
    steps:
      - name: echo name
        shell: bash
        run: echo "hi"
        
      - name: checkCondition1_1
        if: ${{ 1==1}}
        run: echo "working"
        
# success() , failure() , always()
      - name: checkConditionFailure
        if: failure()
        shell: bash
        run: echo "something has failed"
