name: environ
on: [workflow_dispatch]
env:
  global: Value

jobs:
  job1:
    runs-on: ubuntu-latest
    env:
      jobVar: Valuelocal
    shell: bash
    
    steps:
      - name: ENV test
        env: 
          stepVar: step
        run: |
          echo "${global}"
          echo "${jobVar}"
          echo "${stepVar}"
     
    
