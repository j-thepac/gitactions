
name: docker
on: [workflow_dispatch]
jobs:
  myjobname:
    runs-on: ubuntu-latest
    container:
      image: broadinstitute/scala-baseimage:latest
    steps:
      - name: scalatest
        shell: bash
        run: |
          scala
          'print("hi")'
            
